# ─── Legacy reference (kept locally, NOT part of the new app repo) ───
old/

# ─── Dependencies ───
node_modules/
.pnp
.pnp.js

# ─── Expo ───
.expo/
dist/
web-build/
expo-env.d.ts

# ─── Native (Continuous Native Generation — regenerated via `expo prebuild`) ───
/android
/ios
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# ─── Metro / build ───
.metro-health-check*
*.tsbuildinfo

# ─── Env & secrets ───
.env
.env*.local
.env.*.local

# ─── Google Play publishing credentials — NEVER commit ───
# The service-account JSON can publish releases to your Play listing.
*service-account*.json
play-store-credentials.json
google-play-key.json

# ─── EAS / signing credentials — NEVER commit ───
# credentials.json holds raw keystore + key passwords; the keystore signs your releases.
credentials.json
*.keystore

# ─── Logs ───
npm-debug.*
yarn-debug.*
yarn-error.*
*.log

# ─── OS ───
.DS_Store
Thumbs.db

# ─── Editor / Claude local settings (personal, not shared) ───
.claude/
.idea/
.vscode/*
!.vscode/extensions.json

# Native (additional)
.kotlin/
*.pem

# Playwright MCP artifacts + local verification screenshots (never committed)
.playwright-mcp/
pawductivity-*.png
redesign-*.png
proto-*.png
app-*.png
z-*.png

# Local build artifacts + signing secrets — kept in-workspace, never committed
credentials/
dist/
*.aab
*.apk
*.jks
*.keystore
!android/app/debug.keystore
